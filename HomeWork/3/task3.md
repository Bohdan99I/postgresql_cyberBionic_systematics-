# Завдання 3

#### 🧠 Пояснення

Оператор `EXCEPT` використовується для пошуку **різниці між двома наборами результатів** (запитів).  
- `EXCEPT` — повертає **унікальні** записи, які є в першому запиті, але відсутні в другому.  
- `EXCEPT ALL` — повертає різницю **з урахуванням дублікатів**.

---

#### 💾 Приклад

```sql
-- Пошук клієнтів, які не зробили замовлення
SELECT customer_id FROM customers
EXCEPT
SELECT customer_id FROM orders;
```

```sql
-- Те саме, але з урахуванням дублікатів
SELECT customer_id FROM customers
EXCEPT ALL
SELECT customer_id FROM orders;
```
---